Done — `sumSquaredErrors_eq_linearProjectionMSE` is now private. Same commit also fixes a stray orphan docstring that the "accept both" merge resolution left behind (it was failing `lake build` on the merge tip — easy miss on the web UI since it doesn't run Lean). `gram_transpose` and `gram_quadratic_nonneg` both have cross-file callers and `gram_quadratic_nonneg` is a planned reuse target for Ch 5/Ch 7 sandwich/chi-square work, so I left those public — happy to make `gram_quadratic_nonneg` private and move it into `Chapter3LeastSquaresAlgebra.lean` if you'd prefer.
